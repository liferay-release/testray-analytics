"""testray-analytics — additive analytics module for Testray.

Reads Testray over REST, analyzes test results, and writes results back for a
client extension to render. See ARCHITECTURE.md for the design.

Submodules:
  analysis      regression triage (this milestone)
  insights      dashboards / metrics / scoring   (future)
  custom_suite  test triggering / custom suites   (future)
"""

__version__ = "0.1.0"
